# Copy to .env and set real values before deploying
JWT_SECRET=change-this-to-a-long-random-string
ALLOWED_ORIGINS=http://localhost:3000
DB_URL=postgres://parkipal:change-me@db:5432/parkipal
DOMAIN=parkipal.com
EMAIL=leetodd1970@gmail.com
UPSTREAM=api:3000
# Deployment environment (copy to .env and edit)

# Public site domain and ACME email
DOMAIN=example.com
EMAIL=admin@example.com

# API CORS allowlist (must include your site origin)
ALLOWED_ORIGINS=https://example.com,https://www.example.com

# JWT secret for API auth
JWT_SECRET=replace-with-strong-secret

# Postgres credentials
PG_DB=parkipal
PG_USER=parkipal
PG_PASSWORD=change-me
